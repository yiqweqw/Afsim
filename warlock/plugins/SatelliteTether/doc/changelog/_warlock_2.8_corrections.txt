* **(2.8.1)** Removed potential instability when adding tracks to a :doc:`Satellite Tether View<../wkf_plugin/wk_satellite_tether_view>`. (:issue:`AFSIM-1055`)
* **(2.8.1)** Corrected an issue where an open :doc:`Satellite Tether View<../wkf_plugin/wk_satellite_tether_view>` for a platform that is then removed could lead to a crash, either shortly thereafter if other platforms were being tracked, or when the user attempts to open the context menu. (:issue:`AFSIM-1158`)
